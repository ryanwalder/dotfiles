# vi: set ft=sh :

export PAGER="less"
export EDITOR="/usr/bin/vim"
if [[ $(hostname) == "tfu" ]]; then
    export GDK_SCALE=2
    export GDK_DPI_SCALE=05
fi

# Make gpg and git work nicely together
export GPG_TTY=$(tty)

if [[ -f ~/.bash_work ]]; then
    source ~/.bash_work
fi
