#!/usr/bin/env bash

if [[ $(hostname) == 'tfu' ]]; then
    AC="AC"
fi

power_connected() {
    local state=$(cat /sys/class/power_supply/$AC/online)
    if [[ $state == 0 ]]; then
        echo "Not plugged in"
    else
        echo "Plugged in"
    fi
}

power_connected
