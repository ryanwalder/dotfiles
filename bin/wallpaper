#!/usr/bin/env bash

[[ -d ~/.cache/wallpaper ]] || mkdir ~/.cache/wallpaper

image=$(find ~/.wallpapers/ | sed '1d' | shuf -n1)
cached_image=~/.cache/wallpaper/current.png
convert ${image} ${cached_image}
DISPLAY=:0.0 feh --bg-max $cached_image
