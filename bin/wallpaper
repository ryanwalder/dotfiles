#!/usr/bin/env bash

[[ -d ~/.cache/wallpaper ]] || mkdir ~/.cache/wallpaper

image=$(find ~/.wallpapers/ | sed '1d' | shuf -n1)
cp "${image}" ~/.cache/wallpaper/current
DISPLAY=:0.0 feh --bg-max ~/.cache/wallpaper/current
